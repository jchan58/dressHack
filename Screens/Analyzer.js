import React, {useState} from 'react';
import { View, Text, Image, StyleSheet, TouchableOpacity, useWindowDimensions } from 'react-native';

const Analyzer = () => {
    return(
        <Text>This is the Anaylzer Page</Text>
    );
}

export default Analyzer